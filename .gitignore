# Test scaffolding (kept locally, not on the published site)
_test*.js

# Source document
*.docx

# Student PII — assessment schedule with names + IDs stays local only
SEMESTER_SCHEDULE.xlsx
*.xlsx

# Assessment plan stays local — even though it uses anonymous S01..SN codes,
# keep it off git as defence-in-depth against accidental PII leaks.
ASSESSMENT_PLAN.md

# OS / editor artifacts
.DS_Store
.fuse_hidden*
*~

# Office lock files
.~lock.*
~$*

# Smoke test (kept locally)
tools/_smoketest.js

# OpenAI key (never commit)
.openai_key
