This callout maps every button on the widget back to a step in Cantor's proof. Build next row adds another function f sub i to the supposedly-complete table — that is panel one of the visual above, where we assume someone has handed us a list of every function from naturals to zero-one. Show diagonal highlights the diagonal cells f sub i of i — those are the values g will disagree with. Construct g flips each diagonal bit, giving you a function that differs from every row at its own diagonal — that is panel three, the contradiction.
