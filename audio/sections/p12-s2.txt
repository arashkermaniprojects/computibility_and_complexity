Let’s dive into why ADFA, EDFA, and ACFG are decidable, while A sub T M, E sub T M, and EQ sub C F G are not. The key difference lies in how we frame our questions. For ADFA, EDFA, and ACFG, we can use efficient algorithms to determine answers. For instance, with ACFG, we can use a parsing technique like CYK that runs in polynomial time. 

Now, when we shift to EQ sub C F G, we hit a wall of undecidability. This is because comparing two context-free grammars means we have to consider every possible string in the alphabet, which is represented by Σ*. This leads us into a realm of quantifiers that are unbounded, making it impossible to find a finite algorithm to solve the problem. Essentially, small changes in the problem can drastically change whether it's decidable or not. So, while some questions are solvable with clear algorithms, others stretch beyond our computational limits, reminding us just how intricate the landscape of decidability can be.
