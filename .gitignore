# Test scaffolding (kept locally, not on the published site)
_test*.js

# Source document
*.docx

# Student PII — assessment schedule with names + IDs stays local only
SEMESTER_SCHEDULE.xlsx
*.xlsx

# OS / editor artifacts
.DS_Store
.fuse_hidden*
*~

# Office lock files
.~lock.*
~$*

# Smoke test (kept locally)
tools/_smoketest.js
