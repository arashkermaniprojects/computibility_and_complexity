This animation reconstructs the Cook-Levin tableau step by step. We start with an empty n-to-the-k by n-to-the-k grid. Time fills in from the top, row by row, as the verifier runs. The first row is locked in: it is the start configuration with state q zero at the head, the input x written on the left portion of the tape, and the certificate filling the next portion. Each subsequent row is determined by the previous one through the transition function. The animation overlays two-by-three windows as it goes, showing that local consistency between adjacent rows is enough to enforce the entire computation. After the last row, the animation highlights the accept marker — the unique cell where the q-accept state appears. The total formula has three families of clauses: a START clause pinning down row zero, one MOVE clause per two-by-three window asserting δ-consistency, and one ACCEPT clause asserting that q-accept appears somewhere. The total number of clauses is polynomial in n, completing the Cook-Levin reduction.
